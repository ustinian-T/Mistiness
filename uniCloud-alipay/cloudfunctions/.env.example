# uniCloud 云函数环境变量配置模板
# 在 uniCloud 控制台 → 云函数 → 环境变量 中配置以下项
# 本文件仅作参考，不要将真实值提交到版本库

# ── 必填 ────────────────────────────────────────────────

# JWT 签名密钥：随机生成，建议 32 位以上
JWT_SECRET=请替换为随机字符串如：mist-jwt-xxxxxxxxxxxxxxxx

# AES 加密密钥：固定32字节，用于加密用户API Key
# 生成方式：node -e "console.log(require('crypto').randomBytes(32).toString('hex').slice(0,32))"
AES_KEY=请替换为32位字符串如：mist-aes-key-xxxxxxxxxxxxxxxx

# ── AI 服务（可选，有则用系统Key；无则强制用户自备） ──

# 服务端 OpenAI API Key（邀请码用户使用）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx

# 如使用第三方兼容接口，填写 Base URL
OPENAI_API_BASE=https://api.openai.com/v1

# ── 仅初始化时使用，用完立即删除 ────────────────────────

# 管理员初始密码（运行 init-admin 云函数后立即删除此项）
ADMIN_INIT_PASSWORD=请设置一个强密码然后运行init-admin后删除此项
